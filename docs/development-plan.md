# 開発計画・進捗管理

## 進め方の方針

- **設計（インフラ → アプリ）を先に固め、構築はインフラ → アプリ実装の順に進める**
  - Phase D: インフラ設計の一時 Fix → Phase A: アプリ設計の大枠（上流設計）→ Phase 9: アプリ設計（詳細・前倒し）→ Phase 0〜8: インフラ構築（空回し確認・監視・CI/CD まで）→ Phase 10〜13: アプリ実装（10: 実装準備〔冒頭でアプリ設計の最終 Fix〕→ 11: 画像生成バッチ → 12: SNS 投稿バッチ → 13: 定常運用開始。10-2 で展開）→ Phase 14〜17: 収益化に向けた機能拡充（14-1 で展開。上位方針は [docs/strategy/business-strategy.html](strategy/business-strategy.html)）
- **アプリ設計は 2 段階で行う**: 大枠（Phase A）と詳細（Phase 9）に分ける
  - Phase A では仕様の壁打ち・設計書構成の決定・主要方針の骨子までを固め、Phase 9 で詳細化する
  - 経緯 1: 2026-07-06、上流工程に適した生成 AI モデル（Claude Fable 5）の利用期限を機に、Phase 9 の上流部分を Phase A として前倒しした
  - 経緯 2: 2026-07-06、Phase A が想定より早く完了したため、同じ理由で Phase 9（詳細設計）自体も Phase 0 より前に前倒しした。フェーズ名は「Phase 9」のまま維持する（設計書各所の「Phase 9-x で詳細化」参照を有効に保つため）。インフラ構築で得る知見の反映は Phase 10 冒頭の最終 Fix（10-1）で行う
- 各ステップで実際に AWS 上で稼働確認を行い、動作を確認してから次に進む
- 1 ステップ = 1 プロンプトを基本とし、「作る → 確認 → 次へ」のリズムで進める
- 各ステップの完了後、ユーザーが確認方法に従って動作確認を行い、チェックを入れる
- **記録の運用**: ステップ完了時、本ファイルにはチェック + 完了日 + 要点（1〜2 行）のみを記録し、詳細な実施記録（検証内容・決定事項の全文）は [development-log.md](development-log.md) に追記する（計画書の肥大化防止。2026-07-18 に完了フェーズの記録を移管して導入）
- CDK コマンド例では app 内の論理スタック ID（`FoundationStack` など）を指定する。`-c env=prod` により、CloudFormation 上の実スタック名は `Prod-FoundationStack` のように環境名付きで作成される

### 設計 Fix の運用ルール

設計書が Fix しない問題の再発防止ルール（詳細は [docs/index.html](index.html) セクション 4 を参照）:

- 設計書は「**次フェーズの作業に着手できる**」水準で一時 Fix とする。「生成 AI の指摘ゼロ」は Fix 条件にしない
- 生成 AI レビューは**観点を限定**して依頼し、**最大 2 巡**まで
- 指摘は **blocker**（誤り・矛盾・欠落）と**改善提案**に分類し、blocker のみ修正する。改善提案・3 巡目以降の指摘は「設計課題リスト」（本ファイル末尾）に記録する
- インフラ設計書にアプリ仕様を書かない。アプリの論点は [docs/app/index.html](app/index.html) の検討メモへ

## 凡例

- `[ ]` 未着手
- `[x]` 完了
- `[!]` 問題あり・要対応

---

## 完了フェーズのサマリ（Phase D 〜 Phase 15）

設計（Phase D・A・9）・インフラ構築（Phase 0〜8）・アプリ実装と定常運用開始（Phase 10〜13）は 2026-07-06〜07-23 に、動画・リール対応（Phase 14）と勝負セットの投入（Phase 15）は 2026-07-23〜07-30 に完了済み。各ステップの実施記録（確認・備考の全文）は [development-log.md](development-log.md) を参照。

| Phase | ゴール（達成済み） |
|---|---|
| Phase D: インフラ設計の一時 Fix | インフラ設計書（docs/infra/）を一時 Fix し、Phase 0〜8 に着手できる状態にする |
| Phase A: アプリ設計の大枠（上流設計） | アプリ仕様の壁打ちを行い、設計書構成・主要方針の骨子を docs/app/ に記載する |
| Phase 9: アプリ設計（詳細）※前倒し | Phase A の大枠を詳細化してアプリ設計書を完成させ、一時 Fix する（本スキーマ DDL 作成含む） |
| Phase 0: ローカル開発基盤の整備 | CDK と Docker がローカルで動く状態にする |
| Phase 0.5: AWS アカウントのクリーンアップ | 過去の残存資材を棚卸し・削除し、きれいな状態から構築を始める |
| Phase 1: CDK プロジェクト初期化 + VPC デプロイ | CDK で最小のリソース（VPC）を AWS に作れることを確認する |
| Phase 2: FoundationStack の段階的構築（低リスクリソース） | Aurora・DB 準備確認以外の共通基盤リソースを構築する |
| Phase 3: FoundationStack の段階的構築（Aurora + DB 準備確認） | Aurora と DB 準備確認タスクを構築する |
| Phase 4: SNS 投稿バッチの空回し | SnsPostBatchStack でパイプラインが動くことを確認する（業務ロジックなし） |
| Phase 5: 画像生成バッチの空回し | EventBridge → Step Functions → ECS Fargate のパイプラインが動くことを確認する |
| Phase 6: DB 接続の疎通（最小） | ECS タスクから Aurora に接続できることを確認する |
| Phase 7: MonitoringStack | バッチ失敗時にアラーム通知が届く |
| Phase 8: CI/CD パイプライン構築 | GitHub push で自動的にビルド・デプロイされる（イメージは空回し版のまま） |
| Phase 10: 実装準備 | DB スキーマ・ローカル環境が業務ロジック実装に耐える状態になっている |
| Phase 11: 画像生成バッチの業務ロジック実装 | Step Functions 経由で実画像が生成され、S3 + DB に記録される |
| Phase 12: SNS 投稿バッチの業務ロジック実装 | 生成済みの実行が投稿先プラットフォーム（初期スコープ: Instagram）に自動投稿され、重複しない |
| Phase 13: 定常運用の開始 | EventBridge Scheduler による全自動運用が回っている |
| Phase 14: コンテンツ生成への一般化（動画・リール対応） | 画像生成バッチが動画（リール）も生成・投稿できる「コンテンツ生成バッチ」に拡張され、リール 1 日 1 回の全自動投稿が定常運用に入っている（KPI M1 達成） |
| Phase 15: 勝負セットの投入（セット 2） | 勝負セット logic-training-1 が投入され、2 セット・1 日 4 チェーンの動画中心の自動運用が回っている（KPI M2 への挑戦開始） |

---

## 次期計画（Phase 14〜17・XX）: 収益化に向けた機能拡充

2026-07-23 の壁打ちで策定。**上位方針（収益化経路・予算・KPI・プラットフォーム展開方針）は [docs/strategy/business-strategy.html](strategy/business-strategy.html) を正とする**（Phase 14-1 で新設した事業戦略層）。優先順位の考え方: 配信面の拡大より先に「リーチ獲得能力（動画・リール）」→「勝負セット投入」→「効果測定」→「当たりが出てからプラットフォーム展開」の順。

### ステップ別の利用モデル・エフォートレベル

2026-07-24 のトークン最適化検討で策定。方針: **壁打ち・設計 Fix・大型委譲成果物のレビューは Fable 5、それ以外の定型ステップは `/model` で最新の Opus に切り替える**（策定時の既定は Opus 4.8。2026-07-26 の Opus 5 リリースに伴い定型の既定を Opus 5 へ更新。Fable 5 の使いどころは Opus 5 前提で再確認のうえ維持をユーザー決定）。Codex 委譲は terra / high を既定とし、機械的な横展開は luna、大型・新規性の高い実装は sol へ per-call で変更する（CLAUDE.md の Codex 連携ルールを参照）。エフォートは「確実にこなせる最低レベル」を選ぶ。

完了ステップ（Phase 14〜16-4b・16-6・16-7）の割当実績は [development-log.md](development-log.md) の各ステップ記録と git 履歴を参照。残ステップの割当（16-8〜16-12 は 16-7 の設計 Fix 時に展開・割当）:

| ステップ | Claude 側（モデル / エフォート） | Codex 委譲（モデル / エフォート） |
|---|---|---|
| 16-5 効果検証 | Opus 5 / high（指標の解釈で詰まったら Fable 5 へ切替） | なし（インサイト画面の目視のため委譲不向き） |
| 16-8 データモデル拡張（V009） | Opus 5 / high | terra / high（DDL 生成。レビューは Claude） |
| 16-9 insights-batch アプリ実装 | Opus 5 / high（指示書・レビュー。詰まったら Fable 5） | **sol / high**（新サービス実装 + shared 昇格。完了条件 = pytest 全パス） |
| 16-10 インフラ実装・デプロイ | Opus 5 / high（指示書・レビュー） | terra / high（CDK 実装 = 既存スタックのパターン複写） |
| 16-11 再認可・稼働確認・バックフィル | Opus 5 / high（伴走） | なし（ユーザー作業主体） |
| 16-12 指標セット確定の反映（16-5 完了後） | Opus 5 / high（指標選定の判断で詰まったら Fable 5） | terra / high（昇格 DDL・ビュー生成のみ） |
| 17-1 テーマ Fix 壁打ち・17-2 設計 Fix | **Fable 5** / デフォルト | なし |
| 17-3 データ整備・DDL 適用・初期ストック投入 | Opus 5 / high（文言・ナレーション執筆とレビュー伴走） | terra / high（実データ検証・県換算スクリプト。出典の再検証は `codex --search exec`） |
| 17-4 Remotion 本組み・ビルドツーリング | Opus 5 / high（指示書・レビュー。版面の見た目判断で詰まったら Fable 5） | **sol / high**（新規 TSX コンポジション + ツーリング一式 + 方式モジュール + キャプション拡張。完了条件 = pytest 全パス + サンプルネタの 2 尺通しビルド成功） |
| 17-5 セット追加・初期整備（14 ネタ）・稼働開始 | Opus 5 / high（伴走・全数レビューの取り回し） | imagegen（背景 14 枚）+ terra / high（単尺対応の小修正・追加 4 件のデータ検証。他はユーザー作業主体） |
| 17-6 テスト投稿・効果確認 | Opus 5 / high（初速の解釈で詰まったら Fable 5） | なし（インサイト画面の目視のため委譲不向き） |
| 17-7 補充スキル化 + 残り 16 件（実地テスト） | Opus 5 / high（スキル執筆・レビュー伴走） | imagegen（背景 16 枚）+ terra / high（実データ検証。出典の再検証は `codex --search exec`） |
| Phase XX（プラットフォーム展開）調査・着手判断 | **Fable 5** / デフォルト | — |

## Phase 16: インサイト収集と KPI 運用

**ゴール**: 投稿パフォーマンス（再生数・フォロワー等）が自動収集され、KPI 実績（事業戦略書セクション 4.1）が数字で追える

> 2026-07-31 にステップ展開。運用初週の手動インサイト確認で勝負セットのリールの**スキップ率 95%** が判明し、ユーザー方針でフォーマットのテコ入れを前段ブロックとして前倒しした。前段の進行中に方針を 2 度更新: 16-1 で問題ソーシングを**ストックのみ方式**（実行時の LLM 生成・検証チェーンは休止）へ、16-3 完了後（2026-08-08）に**動画の事前生成方式（quiz-prebuilt）への転換**（イラスト〜動画をローカルでビルドして全数人間レビューのうえ S3 に作り置きし、実行時は取得のみの軽量実行にする）をユーザー決定した。経緯・技術的裏付けの全文は [development-log.md](development-log.md) の Phase 16 冒頭と 16-1・16-3b を参照。後段ブロック（インサイト収集バッチ）のステップ展開は当初「16-5 完了時（手動比較で『見たい指標』が固まった後）」としていたが、2026-08-09 に **16-5 と並行しての先行着手**をユーザー決定した。理由: 16-5 は観察期間（カレンダー待ち）が主体で開発帯域と競合せず、後段のうち 16-5 に依存するのは「継続的に見る指標セットの確定」のみ。指標選定に依存しない API 調査・データモデル拡張・実行基盤設計を先行し、指標セットの確定だけを 16-5 完了後に反映する。API 調査で判明する「自動収集できる指標 / 手動でしか見えない指標」の境界は、16-5 で記録すべき項目への入力にもなる。**2026-08-10 追記: 16-5 は前後比較をやめ、切替後（2026-08-08 以降）の投稿だけを母集団とする水準評価へ方針変更した**（切替前の投稿がアーカイブ済みで API から読めないため。16-5 の項を参照）。

### 前段ブロック: 勝負セットのテコ入れ（スキップ率 95% 対応）

- [x] **16-1** テコ入れの設計 Fix（カット構成・キャプション・問題ストック）
  - 備考: 2026-08-01 完了（Fable 5）。ストックのみ方式（生成チェーン休止・在庫切れは LRU 再利用）・16 秒 4 カット版面固定・キャプションのプレースホルダ 5 種・`quiz_stock_items` 新設（V005）を決定し、設計書 5 本を改訂。詳細は [development-log.md](development-log.md) の 16-1 を参照
- [x] **16-2** 問題ストックの初期整備（リサーチ・書き直し・投入）
  - 備考: 2026-08-07 完了。出題路線をユーザー決定で「朝 = 定番なぞなぞ / 昼 = フェルミ推定 / 夜 = とんち・水平思考」へ改訂し、42 問を執筆・機械検証・1 問ずつの人間レビュー（全問承認）のうえローカル + Aurora へ投入（14 問 × 3 組・V005 適用込み）。執筆・レビューの勘所はスキル `/quiz-stock-replenish` に集約。詳細は [development-log.md](development-log.md) の 16-2 を参照
- [x] **16-2b** 方式設計書の改訂（16-2 レビュー中のユーザー決定の正式反映）
  - 備考: 2026-08-07 完了（Fable 5）。カット構成を「つかみ → 考える → カウントダウン〔吹き出し = `hint` 動的〕→ 誘導」へ改訂・L3 は捻った分解型 + 解説 240 字へ拡張・資材置き場 `content/quiz-stock/` の運用ルールを operation.html に明文化・コーチ表情の入れ替え。詳細は [development-log.md](development-log.md) の 16-2b を参照
- [x] **16-3** 生成・投稿パイプラインの改修（実装）
  - 備考: 2026-08-07 完了（実装 = Codex sol 委譲・レビューと動画確認 = Fable 5）。ストック LRU 取得・16 秒版面固定の組版刷新・キャプションのプレースホルダ展開を実装し、pytest 全パス + ローカル ffmpeg で 3 スロット分の MP4 を確認。動画のユーザーレビューで型・難度ピル廃止・`slot_label` を「朝 / 昼 / 夜の脳みそトレ」へ改名（Aurora 反映は 16-4）。詳細は [development-log.md](development-log.md) の 16-3 を参照
- [x] **16-3b** 動画事前生成方式への転換設計（設計 Fix）
  - 備考: 2026-08-08 完了（Fable 5）。V006（`video_s3_key` / `video_audio_asset_id` / `video_built_at`）採用・新方式設計書 [quiz-prebuilt.html](app/generators/quiz-prebuilt.html) 新設（ビルド済み限定 LRU・切替は `generator_name` 1 本で可逆）・gpt-quiz-multicut は実行時休止 = レンダラー転用・ビルドツーリングは `content/video-build/`・S3 は `assets/{set_code}/prebuilt/`（ライフサイクル対象外）・コストモデル改訂（logic-training-1 の生成 AI API 定常費ゼロ）。設計書 7 本を改訂。詳細は [development-log.md](development-log.md) の 16-3b を参照
- [x] **16-3c** 事前生成方式の実装（Codex 委譲）
  - 備考: 2026-08-08 完了（実装 = Codex terra 委譲・レビューと通し確認 = Fable 5）。generator `quiz_prebuilt.py` + テスト・V006 DDL・ビルドツーリング 5 本 + README・週次補充スキルの手順拡張を実装し、pytest 全パス + 1 問通し確認（imagegen → ビルド → S3 → 実行時ローカル動作）。レビューで Aurora 反映 SQL を安定キー方式（id 直指定不可）へ修正。詳細は [development-log.md](development-log.md) の 16-3c を参照
- [x] **16-3d** 初期 42 問の動画ビルドとレビュー・配置
  - 備考: 2026-08-08 完了。V006 を Aurora へ適用 → イラスト 42 枚生成（imagegen 委譲）→ 42 問ビルド → 全数人間レビュー全問 OK → S3 84 オブジェクト配置 + 両環境 DB 反映。配置直前に S3 キー規約の欠陥（環境ローカルな AUTO_INCREMENT 依存）が判明し、業務キー `content_key`（V007 / V008）を導入してキー規約を `assets/{set_code}/prebuilt/{content_key}.mp4` へ改訂。詳細は [development-log.md](development-log.md) の 16-3d を参照
- [x] **16-4** 投稿前レビューと切替（15-14 パターン）
  - 備考: 2026-08-08 完了（Fable 5 が直接実施）。**切替日 = 2026-08-08（前後比較の基準点）**。デプロイ → Aurora 反映 4 点（キャプションテンプレート差し替え・`slot_label` 改名・`generator_name` 切替・生成スケジュール 3 本の再有効化）→ 手動実行で exit 0・26 秒の軽量実行を実証 → 投稿前レビュー GO → リール + ストーリーズ実投稿 success → 3 スロットの定時実行成功を確認し quiz-prebuilt が定常運用入り。詳細は [development-log.md](development-log.md) の 16-4 を参照
- [x] **16-4b** 切替後のフォーマット改善（視線誘導と見切れ解消）
  - 備考: 2026-08-09 完了（Opus 5 が直接実施）。コーチ + 吹き出しのバウンド演出（透過レイヤー分離 + ffmpeg overlay。大バウンド 2 箇所は既存 SE と同期）とイラスト見切れの解消（組版の枠埋めを廃止し比率を保って内接 + imagegen の 3:2 出力をプロンプト明示）を実装し、`build.py --rebuild` で 42 本を再ビルド → 全数レビュー承認 → S3 + 両環境 DB を差し替え（出題キュー・選曲は不変）。詳細は [development-log.md](development-log.md) の 16-4b を参照
- [x] **16-4c** 動画構成の見直し（3 カット化・つかみ帯・ズーム廃止）
  - 備考: 2026-08-23 完了（Opus 5 が直接実施・テスト追随のみ Codex terra 委譲）。2026-08-w3 の補充作業中にユーザーが 3 点を指示: ①カット 1「つかみ」と カット 2「考える」を導入 8 秒へ統合し 4 カット → **3 カット**（吹き出しは固定文「わかったらコメントしてくれ！」）②**つかみをカード上部の帯へ移設**し、文言をスロット別の可変値 `slot_hook`（`prompt_configs.parameters`）に変更（問題ごとの `hook` はキャプション専用として存続）③**全編通しの連続ズームを廃止**。版面変更のため**既存 42 本 + 新規 21 本 = 全 63 本を再ビルド・再配置**した。詳細は [development-log.md](development-log.md) の 16-4c を参照
- [x] **16-4d** 昼スロット（フェルミ推定）の当分停止
  - 内容: 2026-08-24 のユーザー決定。理由: ①昼（L3 フェルミ推定）のスキップ率が高止まりで改善しない ②ユーザー自身が改めて問題を見ても魅力を感じない ③魅力を感じない投稿が定期的に混ざるとフォロワーが増えない、との判断。**廃止ではなく当分停止**（再開判断は将来。noon の Scheduler だけを止め、ストック・スロット定義・BGM・パレット等の資材は残置して凍結する）。朝・夜の 2 スロットは継続。サブステップで詳細化しながら進める:
  - [x] **16-4d-1** 停止の実施: CDK で noon Scheduler（`acps-prod-image-generation-logic-training-1-noon`）を `enabled: false` 化 + テスト追随 → DISABLED を確認（2026-08-06 障害時と同じ「CDK に反映して deploy で戻らない」方式。deploy が classifier にブロックされたため CLI 即時 DISABLE + CDK 反映で実施）
  - [x] **16-4d-2** 設計書への停止中の記載: セット別設計書（sets/logic-training-1.html）を正として停止の経緯・凍結範囲を記載（`#noon-suspended`）し、workflow.html Scheduler 一覧・app/index.html セット一覧・batch-flow.html 方式カタログ・gpt-quiz-multicut.html の L3 補充方針・事業戦略書セットポートフォリオへ注記 + 参照リンクを追加
  - [x] **16-4d-3** 補充運用の変更: operation.html セクション 3 の補充閾値へ停止スロット除外を明記し、スキル `/quiz-stock-replenish` を「補充対象は朝・夜の 2 組のみ」へ改訂（昼の既存在庫は凍結扱い・執筆勘所は再開時の参照用として残置）
  - 備考: 2026-08-24 完了（Fable 5 が直接実施）。noon Scheduler = DISABLED・morning / night = ENABLED を AWS 上で確認。キャプションテンプレートの `#フェルミ推定` ハッシュタグ残置は設計課題リストに記録。詳細は [development-log.md](development-log.md) の 16-4d を参照
- [ ] **16-5** 効果検証（切替後データの水準評価）
  - 内容: **2026-08-10 のユーザー決定で「切替前との前後比較はしない」方針に変更**（切替前の投稿がアーカイブ済みで Graph API から読めず、自動収集データでの比較が成立しないため。経緯は [development-log.md](development-log.md) の 16-11 (5) を参照）。**2026-08-08 以降に投稿されたリール（決定時点で 8 件）とそれ以降の投稿を母集団**として、スキップ率・視聴維持・保存・フォロワーの水準と推移を評価し、事業戦略書の KPI 実績へ記録する。前後比較の代わりに「現行フォーマットの絶対水準が目標に届いているか」「投稿を重ねて改善傾向にあるか」を判断材料にする。視聴維持カーブと View Rate は API 非対応のためアプリ画面での目視（16-6 の分担）。ここで「継続的に見たい指標」を確定し、16-12 の入力とする
### 後段ブロック: インサイト収集バッチ

- 骨子: 第 3 のバッチ（インサイト収集）の設計・実装（A-1 壁打ちから将来拡張として織り込み済み。16-7 の設計 Fix で `post_insights` / `account_insights_daily` の 2 テーブル構成を確定〔[data-model.html](app/data-model.html) セクション 4.11〕・問題別パフォーマンスの突合経路は `posts` → `generation_run_id` → `quiz_items.stock_item_id`〔16-1 整備済み〕）。2026-08-09 のユーザー決定で 16-5 完了を待たず先行着手（並行方針の理由は Phase 16 冒頭の備考を参照）
- [x] **16-6** Instagram Graph API インサイト機能の調査
  - 備考: 2026-08-09 完了（リサーチ = `codex --search exec` 委譲・再検証 + 整理 = Fable 5）。調査基準 v25.0。**スキップ率は `reels_skip_rate`（2025-12 追加）として API 取得可**・視聴維持カーブと View Rate はアプリ画面のみ（= 16-5 手動比較の記録分担が確定）。設計上の主要制約は ①ストーリーズは投稿後 24 時間の取得期限 ②アカウント指標は保持 90 日 = 日次の自前保存が必須 ③追加権限 `instagram_manage_insights` は既存トークンへの再認可 + 新トークン発行が必要。詳細は [development-log.md](development-log.md) の 16-6 を参照
- [x] **16-7** インサイト収集バッチの壁打ち・設計 Fix
  - 備考: 2026-08-10 完了（Fable 5）。壁打ちのユーザー決定 4 点: **①実行は 1 日 2 回（06:00・18:00 JST）** ②メディア追跡は投稿後 14 日（`MEDIA_LOOKBACK_DAYS`） ③格納は **JSON 全量 + 16-5 後に生成カラム / ビューへ昇格**（2 段階 Fix の実現方式） ④初回バックフィルを実施（アカウント日次 90 日 + 既存全投稿 = 8/8 切替の前後比較を自動収集データでも可能に）。既定採用: InsightsBatchStack 新設（既存パターン踏襲・セット別実行）・`post_insights`（UNIQUE (post_id, scheduled_at) で冪等）+ `account_insights_daily`（UTC 日 upsert-once）・`posts` へ UNIQUE (set_id, id) 追加（V009）・`instagram_manage_insights` 再認可を 16-11 に組み込み。設計書 9 本 + CLAUDE.md を改訂。詳細は [development-log.md](development-log.md) の 16-7 を参照
- [x] **16-8** データモデル拡張（V009）
  - 備考: 2026-08-10 完了（DDL 生成 = Codex terra 委譲・レビューと適用 = Opus 5）。`V009__insights_support.sql` を作成し**ローカル MySQL と Aurora の両環境へ適用済み**（`SHOW CREATE TABLE` の差分は環境ローカルな `AUTO_INCREMENT` 値のみ）。設計書に無かった変更を 1 つ追加: `batch_execution_logs.batch_type` ENUM へ `insights_collection` を追加（ENUM 末尾追加。batch-flow.html セクション 4.2 が要求。data-model.html セクション 4.7 に反映）。ローカルで冪等性キー・セット境界の複合 FK・JSON パス抽出（16-12 の昇格の前提）を実挙動で検証し、既存 2 サービスの pytest 214 件で回帰なしを確認。詳細は [development-log.md](development-log.md) の 16-8 を参照
- [x] **16-9** insights-batch アプリ実装
  - 備考: 2026-08-10 完了（メトリクス再ピン留めリサーチ = `codex --search exec` 委譲・指示書とレビュー = Opus 5・実装 = Codex sol 委譲）。`services/insights-batch/`（app 13 モジュール + tests 52 件）と shared 昇格 2 モジュール（`instagram.py` / `sns_secrets.py`）を新設。**API バージョンは v25.0 でピン留め**。実装直前の API 再調査で判明した 2 制約（①`metric_type` / `breakdown` はリクエスト全体パラメータのため 1 メディア 1 リクエストにできない ②未対応メトリクスが 1 つ混ざるとリクエスト全体が 400）に対し、**メトリクスグループ単位の束ね + 未対応メトリクスの自動縮退（個別プローブ + ラン内キャッシュ）**を実装（batch-flow.html セクション 4 に decision として反映）。レビューで検出した blocker 2 件（空メトリクスのサイレント成功・アカウント日次の対象不存在スキップ）を修正済み。ローカル MySQL で冪等性 E2E を含む pytest 全パス。詳細は [development-log.md](development-log.md) の 16-9 を参照
  - 内容: `services/insights-batch/` を新設（アカウント日次 + メディアレベルの収集ロジック・INSERT-or-skip の冪等性・部分失敗続行と終了コード・`X-App-Usage` 監視 + バックオフ・pytest）。Instagram クライアントの汎用層（HTTP / エラー処理）と SNS Secret 名規約を `shared/acps_shared` へ昇格して利用する（sns-post-batch の載せ替えは行わない — 設計課題リストに記録）。API バージョンは v25.0 系でピン留めし、メトリクス名を現行リファレンスで再確認する。処理仕様の正は [batch-flow.html](app/batch-flow.html) セクション 4
- [x] **16-10** インフラ実装・デプロイ
  - 備考: 2026-08-10 完了（CDK 実装 = Codex terra 委譲・指示書とレビュー = Opus 5・デプロイ = ユーザー承認のうえ実行）。FoundationStack へ ECR `insights-batch` を追加し、**InsightsBatchStack を新設して本番へデプロイ済み**（TaskDef `acps-prod-insights-batch` rev 1・SFN `acps-prod-insights-collection-sfn` ACTIVE・Scheduler 4 件〔セット × 06:00 / 18:00 JST〕すべて ENABLED・ScheduleGroup / DLQ・CI/CD パイプライン）。MonitoringStack はアラーム 11 → 18 件（既存 11 件の論理 ID は不変）。初回のみ insights-batch イメージを手動 build / push（タグ `ff08b7d471dc`）。**Scheduler 4 件はデプロイ後に全件 DISABLED へ変更済み**（16-11 の再認可を当日中に実施できないためのユーザー決定。CDK の `insightsCollectionSchedulesEnabled` フラグで停止 — 16-11 完了時に `true` へ戻して deploy する）。詳細は [development-log.md](development-log.md) の 16-10 を参照
- [x] **16-11** 再認可・稼働確認・バックフィル
  - 備考: 2026-08-10 完了（Opus 5。ブラウザ操作はユーザー・検証と修正・デプロイは Claude）。`instagram_manage_insights` 込みで再認可し 2 件の Secret を更新（`data_access_expires_at` 2026-10-25 → **2026-11-08**・リマインダーは 2026-11-01 へ）。両セットの手動実行が成功し、**リール 25 投稿 + アカウント日次 90 日 × 2 アカウントを収集**（`reels_skip_rate` の実データ取得を確認）。疎通確認で**閲覧者不足によるインサイト非開示（`(#10) Not enough viewers`）が失敗計上され恒常アラーム化する不具合**を発見し、スキップ扱いへ修正のうえ再デプロイ。バックフィルの結果、**8/8 切替前の logic-training-1 リール 16 件はアーカイブ済みで Graph API から読めず自動収集できない**ことが判明（16-5 の前後比較は手動記録が正。アーカイブ = 削除と同じ扱いになる点は operation.html 6.2 に明記）。Scheduler 4 件は ENABLED へ復帰し**インサイト収集が定常運用入り**。詳細は [development-log.md](development-log.md) の 16-11 を参照
- [ ] **16-12** 指標セット確定の反映（**16-5 完了後**）
  - 内容: 16-5 の評価結果から「継続的に見る指標セット」を確定し、生成カラム / ビューへの昇格（DDL）と KPI 転記運用（operation.html セクション 6 への追記・事業戦略書 KPI 実績表との接続）を確定する（2 段階 Fix の後段）

---

## Phase 17: 都道府県ランキングセット（第 3 セット・Remotion 生成方式）

**ゴール**: 新セット「都道府県ランキング」（名称仮）が新規 Instagram アカウントで定常運用に入る

> 2026-08-09〜08-10 の事前検証で方向性を確定し、2026-08-10 にステップ展開（Phase 16 後段ブロックと並行）。検証成果物は `plans/ranking-set-research/` — ネタ出し 31 件 + 家計調査の県換算ルール（neta-30.md）・参照動画分析（reference-analysis.md）・Remotion プロトタイプ 2 種〔ランキング行型 / 地図ルーレット型 + テーマ別背景〕（remotion-proto/）。決定済みの方針:
> ①テーマ = 都道府県ランキング。判定基準「下位の県の人が笑って自虐できるか」（× 例: 所得・離婚率・学力）②家計調査（県庁所在市・政令市）は世帯数加重平均で県値へ換算し一貫して都道府県ランキングとする ③生成方式 = Remotion（React/TSX → Docker レンダリング）によるローカル事前ビルド（quiz-prebuilt と同型の作り置き + 全数人間レビュー）④演出 = 白抜き日本地図 × ルーレット型を軸に検討 ⑤テーマ別背景イラスト（imagegen 事前生成・スタイル固定プロンプト + 減光オーバーレイ）⑥キャラクターを作る（役割・声は 17-1 で Fix）⑦ナレーション導入前提（セリフ単位 TTS をタイムライン定数へフレーム同期。手作業の音合わせなし）⑧尺は 16 秒に固定せず 2 尺（16 秒 / 20〜30 秒）のテスト投稿で確定（**→ 17-1 で 20 秒 / 30 秒・両方フルナレーションに修正。16 秒版面は廃止。→ 2026-08-24 に 30 秒で確定、2 尺テスト投稿は中止**） ⑨地図 SVG はプロトの GFDL 素材から本番投入前にパブリックドメイン素材（Natural Earth 等）へ差し替え

- [x] **17-1** テーマ Fix 壁打ち（15-2 相当）
  - 内容: キャラクターの役割・性格・声（TTS 選定）・トーン・パレット・背景方針・演出フォーマット（地図ルーレット型 / ランキング行型の使い分け）・尺方針・セット名 / `set_code`・アカウント設計（新規 IG アカウント）を Fix する。事業戦略書のセットポートフォリオ・予算・KPI への新セット追記もここで行う
  - 備考: 2026-08-10 完了（壁打ち 3 巡 + キャラ試し生成 3 巡〔Codex imagegen〕+ パレット版面モック比較〔Remotion 実レンダリング〕+ VOICEVOX 4 候補試聴）。決定: フォーマット軸 = TOP5 地図ルーレット型（全県塗り型は 17-2 で設計のみ・行型は部品へ吸収）/ 尺 = **20 秒・30 秒の 2 尺・両方フルナレーション（方針⑧を修正）**/ キャラ =「**表彰台五郎**」（表彰台擬人化 + 赤鉢巻 + マイク・1 位発表時のみ軍配。ナレーター専任 + 画面隅常駐）/ 声 = **VOICEVOX 白上虎太郎** / パレット = **和モダン系**（生成り × 藍 × 朱 × 金）/ 背景 = ネタごとに imagegen 事前生成・モチーフはテーマ由来（スタイル固定はレイアウト枠のみ = 生成り地・四隅寄り淡色・中央無地）/ `set_code` = **`pref-ranking-1`** / アカウント =「表彰台五郎の都道府県ランキング」（`daigoro_ranking` 第一候補）。壁打ち記録 [requirements-notes-set3.html](app/requirements-notes-set3.html) を新設し、事業戦略書（ポートフォリオ・コスト・KPI 実績）へ追記済み。詳細は [development-log.md](development-log.md) の 17-1 を参照
- [x] **17-2** セット別設計書・生成方式設計書の作成（設計 Fix）
  - 内容: 3 層構造ルールに従い `sets/` に 1 本・Remotion 事前ビルド方式を `generators/` に 1 本新設。データモデル（ランキングストックのテーブル設計・県換算の正式反映）・キャプション・スケジュール設計を含む
  - 備考: 2026-08-10 完了（Fable 5・Codex 委譲なし。2 セッション）。壁打ち 3 巡（動画設計 / データモデル・ビルドパイプライン / 運用設計）で 17-1 引き継ぎ論点 8 件を全件消化。主な決定: 方式名 = **`ranking-prebuilt`**（[generators/ranking-prebuilt.html](app/generators/ranking-prebuilt.html) 新設）/ タイムライン 2 尺（20 秒 = 600f・30 秒 = 900f）+ TTS は**尺別固定アンカー + 予算検査** / パレット正式値 = wamodernA + 金の沈み対策規定 / **`ranking_stock_items`・`ranking_items` 新設（V010 作成済み・使い捨て MySQL で V001〜V010 の通し適用を検証済み。両環境への適用は 17-3）** / **スケジュール = 1 日 2 回（昼 12:30 / 夜 20:00 JST）・2 尺検証はスロット分離（昼 20 秒 / 夜 30 秒）+ 検証後半スワップ** / キャプション = 結果再掲型 + ランキング系プレースホルダ 5 種 + VOICEVOX クレジット常設 / 在庫水位 28 件・週 14 ネタ補充。**新情報: 次セット候補として英語圏展開（同内容の英語アカウント）の計画があり、再利用要件 4 点（背景の文字焼き込み禁止・文言 props 化・TTS アダプタ層・ネタデータの言語非依存）を方式設計書に明記**。設計書 8 ファイル（新規 2 + 共通 4 + 記録 2）+ V010 を作成・更新し、観点限定レビュー 1 巡で blocker 2 件（検証仮説の出典表記・単位フィールド `value_suffix` の未定義）を修正。詳細は [development-log.md](development-log.md) の 17-2 を参照
- [x] **17-3** データ整備・DDL 適用・初期ストック投入（第 1 バッチ 10 件） — 完了日: 2026-08-11
  - **執筆・レビュー・投入の勘所は [content/ranking-stock/pref-ranking-1/WRITING-NOTES.md](../content/ranking-stock/pref-ranking-1/WRITING-NOTES.md) に集約した**（17-7 でスキル化する際の一次資料。第 2・第 3 バッチもこれを見ながら書く）
  - 残り 20 件（第 2・第 3 バッチ）は当初「スキルの実地テストを兼ねて 17-7 で実施する」としていた（ユーザー判断 2026-08-11）が、**それだと 17-5（初期 30 ネタのビルド）が着手できないため 17-5b（稼働と並行）へ移管した**（ユーザー判断 2026-08-13。17-5 の前提を参照）
  - 進捗（2026-08-10）: **V010 を両環境へ適用済み**（ローカル MySQL / Aurora。`SHOW CREATE TABLE` の定義一致を確認）。**県換算ツーリング `content/ranking-stock/pref-ranking-1/common/` を新設**（家計調査 rank01〜14.xlsx の取得・パース、世帯数ウェイト 52 市の静的テーブル〔令和2年国勢調査 第6-3表の二人以上一般世帯数〕、加重平均による県換算 CLI。pytest 14 件パス）。**VOICEVOX + 白上虎太郎の規約証跡を `content/video-build/pref-ranking-1/LICENSES.md` に整備**。**第 1 バッチ 10 件のデータ検証・文言/ナレーション執筆・投入 SQL 生成まで完了**（`content/ranking-stock/pref-ranking-1/2026-08-initial/`）。残り: ユーザーの人間レビュー → 両環境投入（**`batch_sets` へのセット行登録が前提** = STATUS.md 参照）→ 第 2・第 3 バッチ（残り 20 件）
  - 進捗（2026-08-11）: **第 1 バッチの人間レビュー 1 巡目を実施し、指摘を全件反映**。①`source_display` に単位の基準を明示（家計調査 8 件。「二人以上世帯の年間支出」。画面の出典行帯とキャプションの両方に出る唯一のフィールドであるため、`subtitle`〔30 秒版のみ表示〕ではなくここに置いた）②タイトルを全件「〜大好き都道府県」に改訂（009 温泉のみ据え置き。006 は 008 喫茶代との区別のため「おうちコーヒー」）③**ナレーションに煽り cue `teaser` を新設**し、5〜2 位の呼び込みと県名を 1 本の cue に統合（[ranking-prebuilt.html](app/generators/ranking-prebuilt.html) セクション 8.2/8.3 の decision。総尺・1 位シーン・結果総覧・締めの配分は不変）④レビューシートに**表示文言フィールドの使われ方**（どこに出るか）をレビューの前提として常設 ⑤006 の trivia を家庭内消費の説明に修正・010 の hook の丸めを是正 ⑥**締めのセリフを「呼びかけ」から「中身のある一言」に変更**（20 秒版 `outro` = 「みんなはわかったかな？」全ネタ共通 / 30 秒版は県名羅列の `recap` を廃止し `recap` + `outro` を **`closing` 1 本**へ統合してネタの含意を語る）。⑦**`hook` / `trivia` の文体を表彰台五郎の一人称（タメ口の実況調）に統一**（キャプションだけ中立の解説調だと書き手が入れ替わって見えるため。**キャプションの文体規定が設計書に無かった**のが原因なので [sets/pref-ranking-1.html](app/sets/pref-ranking-1.html) セクション 5 に規定として明記）。`validate.py` は cue 体系の変更に追随し、書式の骨格（大好き / 都道府県 / 第 / 位）だけ実読みを与えて全 10 件パス。**全 10 件がユーザー承認となり、両環境へ投入完了**（ローカル MySQL / Aurora。`(title, content_fields, ranking_data, narration, source_note)` の MD5 が全行一致）。投入時に、先行登録した `batch_sets` の `pref-ranking-1` 行の `name` が**ローカルだけ二重エンコードで文字化け**していたのを発見・修正（`--default-character-set=utf8mb4` 無しで INSERT したため。2026-07-29 / 15-10 記録の既知の落とし穴の再発）。**同じ原因で `fantasy-animals-1` の `name` もローカルだけ文字化けしている**（旧セットのため未修正）。Aurora 投入用に再利用可能な `common/apply_aurora.py`（Data API・文字列内セミコロン対応の分割・`DatabaseResumingException` リトライ）を新設した
  - 内容: V010 の両環境適用 → 県換算ツーリング（世帯数の静的テーブル + 換算スクリプト。`content/ranking-stock/pref-ranking-1/common/`）と出典検証 → ネタ帳 30 件の実データ検証（出典 URL + データ年の突合・TOP5 再計算）→ VOICEVOX 本体 + 白上虎太郎の規約証跡整備 → 初期ストックの執筆（文言 + 尺別ナレーション台本）・人間レビュー・両環境投入（動画ビルドは 17-4 のツーリング完成後 = 17-5）。運用手順の正は [operation.html](app/operation.html) セクション 3（ランキングストック）
- [x] **17-4** Remotion 本組み実装・ビルドツーリング — 完了日: 2026-08-13
  - **サブステップに分割した（2026-08-11）**。分量が大きく、版面デザインの Fix が後続すべての前提になるため
    - [x] **17-4a** 版面デザインの作り込み（モック比較で Fix） — 完了日: 2026-08-11
    - [x] **17-4b** 素材の正式化（PD 地図差し替え・五郎アセット正規化・フォント選定） — 完了日: 2026-08-11
    - [x] **17-4c** コンポジション本組み（30 秒版タイムライン・後ろ合わせアンカー・props の最終形） — 完了日: 2026-08-11
    - [x] **17-4d** TTS アダプタ + 予算検査 + BGM ミキシング確定 — 完了日: 2026-08-11
    - [x] **17-4e** ビルドツーリング 5 段工程（レビューシート・`--rebuild`） — 完了日: 2026-08-12
    - [x] **17-4f** 方式モジュール `ranking_prebuilt.py` + キャプションのランキング系プレースホルダ（sns-post-batch） — 完了日: 2026-08-13
  - **17-4a の成果（2026-08-11）**: Remotion プロジェクトを `plans/`（git 管理外）から **`content/video-build/pref-ranking-1/remotion/` へ移設**し、実データ（001 ぎょうざ）+ 実背景でモック比較を 3 巡した。決定は方式設計書 [ranking-prebuilt.html](app/generators/ranking-prebuilt.html) セクション 8.2 の decision（17-4a）とセット別設計書 [pref-ranking-1.html](app/sets/pref-ranking-1.html) セクション 8 が正。要点: **もう一色を藍 → 淡墨茶へ差し替え**（藍は生成りに合わないとのユーザー判断。えんじ / 松葉 / 墨茶 3 段の比較を経て確定）/ 未発表枠の廃止・下端固定の積み上げ / バー廃止 + `value_prefix`（「年間◯◯円」）新設 / 確定県名が地図 → 行へ飛ぶ・**地図上の順位ピンは不採用** / タイトル帯 2 段（`labels.setLabel` = 「都道府県ランキング TOP5」）/ 0 秒から完成した版面を出す / 冒頭のみ音と字幕を対応させない。**バグ 1 件を検出・修正**: 地図 viewBox を陸地に切り詰めた際に九州南部（鹿児島の南端は viewBox の y=1000）と南西諸島インセットが欠けており、鹿児島・沖縄が上位に来ると発表の瞬間が見えなくなっていた（端の県 3 件でのビルド検証を追加）
  - **17-4b の成果（2026-08-11）**: 決定は方式設計書 [ranking-prebuilt.html](app/generators/ranking-prebuilt.html) セクション 8.2 の decision（17-4b）とセット別設計書 [pref-ranking-1.html](app/sets/pref-ranking-1.html) セクション 7〜8、証跡は `content/video-build/pref-ranking-1/LICENSES.md` セクション 4〜5 が正。要点: ①**地図を Natural Earth 10m admin-1（PD）へ差し替え**（GFDL 由来のプロト素材を廃止。`scripts/build_japan_paths.py` が取得・投影・インセット移設まで行い `japanPaths.ts` / `prefCentroids.ts` を自動生成）。**素材側の誤り 1 件を発見・補正**: Natural Earth は**奄美群島を沖縄県に割り当てている**ため、そのままだと「鹿児島 1 位で奄美が塗られない」内容の誤りになる（目印の島 21 件の県割当をビルド時に毎回検査する仕組みを入れた）。インセットは南西諸島を**ひとまとまりで相似変換**する方式に変え、縮尺は実ジオメトリに合わせて 0.75 → **0.68**（点線 L 字の枠と占有領域の見た目は維持）②**五郎のポーズ 3 種を正式アセット化**（1160x1220 の共通キャンバス・共通倍率。試作は 1 枚ごとに寸法が最大 29% 違い、ポーズ切替でキャラが伸縮していた）。S3 `assets/pref-ranking-1/goro_*.png` へ配置、`CHARACTER` は 17-4a の見た目を保つ値へ更新 ③**フォントを Zen Kaku Gothic New（SIL OFL 1.1）へ Fix**（4 案を実データでレンダリング比較 → ユーザー判断。見出し = Black / 本文 = Medium・Bold。同一 family へ複数ウェイトを登録すると細い字面が選ばれる事象があり、見出しは 1 ファイル 1 family で持つ実装にした）④**投入済み 10 件へ `value_prefix` を追記**（17-4a 新設フィールド。家計調査 8 件 = 「年間」/ 温泉・社会生活基本調査 = null。`common/convert.py` の `meta.prefix` → `generate.py` が転記する単一ソース化も実施し、両環境へ適用 + MD5 一致を確認）。20 秒版の通しビルドが成功することを確認済み
  - **17-4c の成果（2026-08-11）**: 決定は方式設計書 [ranking-prebuilt.html](app/generators/ranking-prebuilt.html) セクション 8.2 / 8.3 の decision（17-4c）が正。要点: ①**30 秒版タイムラインを実装**（導入 105f → 煽り 60f → 5〜2 位 各 105f〔スピン 30f + 停止・行追記 30f + コメント 45f〕→ 1 位 165f → 結果総覧 90f → 締め 60f = 900f）。順位シーンの前半は 20 秒版と同じ配分にして 2 尺でテンポを揃えた ②**タイムライン定数の単一ソースを Python 側へ移した**（`scripts/build_timeline.py` → `src/timeline.json` は生成物・`timeline.ts` は型付けのみ）。アンカー解決と予算検査を行うビルドツーリング（17-4d/e）が Python のため、TS と数値を二重管理すると必ずずれる ③**後ろ合わせアンカーと予算検査を純関数として実装**（`cue_anchors` / `resolve_cue_frames`。順位 cue は「県名の発話開始 = 停止フラッシュ」で逆算し、呼び込み 30f・県名 30f が予算。違反は head/tail の 2 方向 + missing/unknown を例外なしで全件返す）④**props の最終形**（`duration` 必須 + `calculateMetadata` で尺の食い違いを構造的に排除、cue に実測長 `frames`）⑤**全国平均プレートの固定ラベル「全国平均」を廃止**（`subtitle` は自己完結した表示文字列で、009 温泉「全国の源泉は27,899本」では固定ラベルが**内容の誤り**になっていた。`WRITING-NOTES.md` とレビューシートの「30 秒版のみ表示」記述も実態〔両尺の序盤に出る〕へ修正）。pytest 8 件 + `tsc --noEmit` + **2 尺の通しビルド**（600f / 900f）成功
  - **17-4d の成果（2026-08-11）**: 決定は方式設計書 [ranking-prebuilt.html](app/generators/ranking-prebuilt.html) セクション 8.3 の decision（17-4d）とセット別設計書 [pref-ranking-1.html](app/sets/pref-ranking-1.html) セクション 6〜7 が正。要点: ①**TTS をアダプタ層 `scripts/tts.py` に閉じた**（`CueRequest` → `CueAudio` が固定インターフェース。英語圏展開の再利用要件③）。**`name_offset_frames`（県名の発話開始）の実測方法を確定**: cue の `audio_query` のモーラ長を積み上げ、県名は単体クエリのモーラ列で右端一致検索する。全長は WAV 実測を使い、積み上げ予測との差が 0.08 秒超なら異常終了（エンジン更新の検知）②**疑問形の語尾上げが `audio_query` に現れない事実を発見・モデル化**（`enable_interrogative_upspeak` 既定有効。合成時に末尾母音がもう 1 つ足される）。補正なしだと `teaser` 等の疑問形 cue が全件 0.15 秒ずれ、整合検査に全件引っかかっていた ③**予算検査の頭側の判定を「シーン境界の予算」から「直前 cue との重なり」へ変更**（17-4c の固定予算は参考値として残す）。実測すると 5〜2 位の呼び込み部分は 29〜38f あり 17-4c の 30f を**第 1 バッチ全件が超過**する一方、実音声どうしの重なりは最悪 3f しかなく、シーン境界の予算が実態に対して厳しすぎるだけだった（cue 間の最小ギャップ 2f・県名の遅れ上限 8f）④**第 1 バッチ 10 件 × 2 尺（cue 220 本）を実機 VOICEVOX で合成し、話速 1.1 のまま違反 0 件**（県名の遅れは 80 本中 65 本が 0f・最大 6f）。合成 + 検査 + props 解決の CLI は `scripts/build_narration.py`（違反時は props を書かず終了コード 1・manifest キャッシュ・`--auto-speed` は上限 1.2）⑤**BGM ミキシングを実測校正**: コンポジションは**相対バランスだけ**（BGM = ナレーションの 18 LU 下・ダッキングなし・フェード）を決め、**配信レベルはレンダリング後の ffmpeg loudnorm**（`scripts/normalize_loudness.py`。I=-14 LUFS / TP=-1.5 dBTP・映像は再エンコードしない）で揃える方式にした。ナレーションのみの出力が -23.9 LUFS と静かで、Remotion にラウドネス正規化の機能がないため。2 尺の通しビルド → 正規化まで実測（-15.0 / -14.8 LUFS・TP ≤ -1.1 dBFS）。pytest 26 件 + `tsc --noEmit` 成功 ⑥**ユーザーの実物確認を受けた追補**: 「順位間の間がなさすぎでメリハリがない」（実測の最小間隔 2f）に対し、**既定話速を 1.1 → 1.2（上限）へ引き上げ**（`--auto-speed` は余地がゼロになったため削除）+ **総尺を変えずにシーン配分を組み直し**、最小間隔を **15f / 13f**（典型 0.5〜0.9 秒）にした。発話密度が 79% あり、これ以上はセリフ短縮が必要（`WRITING-NOTES.md` に「各 cue は上限の 2 割減」を追記）⑦**声を Fix**: 「棒読みに聞こえる」との指摘に対し、VOICEVOX の可変項目を洗い出して実音声サンプルを 2 巡提示 → **抑揚 1.9**（話者「ふつう」12・音高は既定のまま）をユーザー選定。合成キャッシュが抑揚の変更を検知しない穴も修正（識別子に話者・抑揚を載せた）
  - **17-4e の成果（2026-08-12）**: 決定は方式設計書 [ranking-prebuilt.html](app/generators/ranking-prebuilt.html) セクション 8.1 の decision（17-4e）が正、手順の実行例は `content/video-build/pref-ranking-1/README.md` が正。要点: ①**5 段工程を `content/video-build/pref-ranking-1/` 直下のスクリプト群として実装**（quiz の `logic-training-1/` と同じ構成・同じ名前。`build.py --list` → `export_prompts.py` / `intake.py` → `build.py` → `review_sheet.py` → `publish.py`）。ツーリング本体は WSL ホストで動かし、**Docker が要る処理だけを外に出す**（Remotion / ffmpeg / PIL）②**BGM の取り違えバグを修正**: 通常ビルドが常に LRU 選曲していたため、片方の尺を先に publish 済みの行の残り尺を後からビルドすると別の曲が選ばれ、DB の `video_audio_asset_id` が後発の曲で上書きされて**既存動画の実 BGM と食い違う**（2 尺共通 1 曲の設計とミュート検知運用が壊れる）。行に記録があれば `--rebuild` でなくても引き継ぐようにした ③**Aurora SQL の NULL 上書きを塞いだ**（音源が Aurora に無いと `video_audio_asset_id` を静かに NULL 化していた。`EXISTS` ガードで更新行数 0 として気づける形に）④**背景プロンプトの画風固定行の誤りを発見・修正**: 「生成り地・**藍**・金」のままだった（17-4a で 藍 → 淡墨茶へ差し替え済み）。ツーリングと設計書セクション 4 の両方を修正し、**文言の正はツーリング側**と確定 ⑤**実機で 5 工程を通し確認**（001 ぎょうざの 2 尺ビルド -15.1 / -15.0 LUFS、レビューシート、publish の dry-run と `--no-bgm` 拒否）。pytest 19 件（新規）+ 既存 remotion 26 件パス。BGM 込みの本番ビルドと全数レビューは 17-5
  - **17-4f の成果（2026-08-13）**: 設計は 17-2 で Fix 済みのため**設計どおりの実装のみ**（新規の設計判断なし。正は [ranking-prebuilt.html](app/generators/ranking-prebuilt.html) セクション 2〜7 と [batch-flow.html](app/batch-flow.html) セクション 3.3 の decision）。要点: ①**方式モジュール `services/image-batch/app/generators/ranking_prebuilt.py` を新設**し `REGISTRY` へ `ranking-prebuilt` を登録（実行時は S3 GetObject と DB のみ。スロット解決は `gpt_quiz_multicut.resolve_slot` を再利用し、`_parse_parameters` だけランキング用に別実装。尺 → 尺カラムの解決は許可リストの dict 経由で SQL 組み立てに外部値を通さない）②**当該尺ビルド済み限定の LRU 取得 + 防御的検査**（0 件は異常終了、再利用と未ビルド在庫は WARNING。`title` / `format` / 必須 `content_fields` 4 キー / `ranking_data` の rank 1〜5 一意 / `video_audio_asset_id` 非 NULL を検査。`value_prefix` 等の版面専用フィールドは検査せず保持）③**キャプションを 2 系統へ拡張**（`ranking_items.py` + `models.RankingItem` 新設。既知トークンは両系の和集合 9 種で和集合外は DB を読む前に異常終了、**どちらの系で展開するかは当該実行に存在する行で決まる**、系に属さないトークンは取り違えとして異常終了、トークンなしは素通し）。pytest は image-batch 122 件・sns-post-batch 112 件パス。**実 AWS での稼働確認は 17-5**（セット登録・初期ビルド・Scheduler 追加が揃ってから）
  - **17-3 のプレビュー動画から出た版面の申し送り（2026-08-10）**: 17-3 のネタ判断用に 20 秒版プレビューを 3 本ビルドした（`plans/ranking-set-research/remotion-proto/` の `PrefRankingPreview` + `scripts/build_preview.py`。実データ + VOICEVOX ナレーション + imagegen 背景）。ユーザー評価は**「想像よりかなり品位が低い」**。原因は「17-4 で作り込む要素（BGM・SE・正式キャラアセット・PD 地図・フォント選定・モーションの詰め・色調）が未投入」だが、**現行版面の延長では解消しない**ため、17-4 は実装着手の前に**版面デザインの作り込み**を独立した工程として置く。観察された具体的な課題:
    - **動きが乏しい**: 演出はスピンと停止フラッシュだけで、キャラは静止画の差し替えのみ。20 秒のうち大半が「静止した地図 + 未発表の点線枠」になる
    - **未発表の「？？？」枠が 5 行**あり、序盤の画面が寂しい（枠を出さずに 1 行ずつ生やす等の再検討）
    - **視線が分断**: 地図（上）と順位行（下）が離れ、確定の瞬間に目が行き来する
    - **値の二重表現**: 順位行がバーの長さと数値の両方で値を示していて情報が重い
    - **背景が薄すぎてほぼ無地に見える**（減光 3 層が効きすぎ。中央の減光率は要再調整）
    - **出典行が 2 行に折り返して野暮ったい**（`source_display` の短縮ルールか帯の設計が必要）
    - **タイトル帯は 1 行に収める制約がある**（2 行になると吹き出し・サブタイトルと衝突。プレビューでは 66px + `nowrap` で対処）
    - **1 位発表後の締め 3.0 秒が単調**（バーストが 66f 残った後、動きがない）
  - **17-3 のナレーション改訂から出た実装制約（2026-08-11）**: 煽りシーン（2.0s）の追加と 5〜2 位 cue の統合に伴い、①順位シーンのアンカーは**後ろ合わせ**（統合 cue の県名部分の発話開始が停止フラッシュに重なるよう実測長から逆算）②順位シーンのキャラ吹き出しは**停止フラッシュ以降のみ**表示（統合 cue が県名を含むため、スピン中に出すと結果が先にばれる）。詳細は [ranking-prebuilt.html](app/generators/ranking-prebuilt.html) セクション 8.3 の decision
  - **17-3 で判明した実行環境の知見（ツーリング実装の前提）**: ①WSL には Chrome の共有ライブラリが無く、**Remotion のレンダリングは Docker 経由が必須**（`Dockerfile.render` を踏襲）②背景画像が 2MB 級の PNG だとフォント読み込みが `delayRender` タイムアウトする。**JPEG 化 + `--concurrency=3` + `--timeout` 延長**で安定した ③VOICEVOX は `voicevox/voicevox_engine:cpu-ubuntu20.04-latest`・**話者 ID 12（白上虎太郎・ふつう）**・`speedScale=1.1` / `intonationScale=1.2` で、17-3 の 20 秒版ナレーションは**全 cue が予算内**（実測）
  - 内容: 地図 SVG の PD 差し替え（素材要件は [ranking-prebuilt.html](app/generators/ranking-prebuilt.html) セクション 8.2 の warn）・コンポジション本組み（2 尺タイムライン・パレット・キャラ / インセット・画面文言の props 化）・TTS アダプタ + 予算検査・五郎アセット正式化（リファレンスシート方式）・背景画風行と BGM ミキシングの確定・ビルドツーリング 5 段工程（レビューシート・`--rebuild` 含む）・方式モジュール `ranking_prebuilt.py` + キャプションのランキング系プレースホルダ実装（sns-post-batch）。完了条件 = pytest 全パス + サンプルネタの 2 尺通しビルド成功
- [ ] **17-5** セット追加・初期整備（14 ネタ）・稼働開始
  - **前提を再度見直した（2026-08-24、ユーザー判断。08-13 の「10 件で稼働開始 + 追いかけ補充」を置き換え）**: ①**投稿頻度は 1 日 1 回**（夜 20:00 JST の evening スロットのみ。17-2 の「1 日 2 回」から変更）②**尺は 30 秒で確定**（17-6 で予定していた 2 尺テスト投稿は行わない。20 秒版のタイムライン・コンポジション・方式モジュールの 20 秒対応は実装済みのまま**休眠**とし、増便・尺再検討時の保険として残す〔削除しない〕）③**稼働開始条件 = 14 投稿分（14 ネタの 30 秒版）がビルド・全数レビュー済みであること**（1 日 1 投稿で約 2 週間分。10 件スタート案にあった「6 日目から同一動画が再投稿される」問題は解消）
  - **波及**: 消費が週 14 → **週 7 ネタ**に半減（在庫水位 28 → **14 件**・週次補充 14 → **7 ネタ**目安）。ビルド・レビュー対象は 1 ネタ 1 本になる。**17-7 を 17-6 の後に置いていた理由（勝ち尺の確定待ち）が消滅**したため、17-7 は 17-5 完了後すぐ着手可へ変更
  - **17-4e で決めた前提（2026-08-12、ユーザー判断）**: ①**既存の背景 3 枚（001 / 002 / 009）は作り直す**（「藍」時代のプロンプト生成物のため。14 枚すべて新プロンプト〔`export_prompts.py` が正〕で生成する）②承認はネタ単位（単尺化により 1 ネタ = 30 秒版 1 本）③S3・ローカル MySQL・Aurora の反映は `publish.py` が一気に行う（Aurora 自動適用が既定）
    - [x] **17-5a** 単尺・1 日 1 回化の反映 + 追加ストック 4 件の投入（計 14 ネタへ） — 完了日: 2026-08-25
      - 内容: ツーリング・資材の単尺対応（`validate.py` の narration 検査が 20 秒版 cue を必須にしているため **30 秒版のみで通る形へ緩和**〔既存 10 件の 20 秒版台本は残置。新規執筆は 30 秒版のみ書く〕・`build.py` の対象尺既定 `20s,30s` → `30s`・`WRITING-NOTES.md` へ単尺方針を追記）→ ネタ帳から 4 件を選定（17-3 の採否ルール + 既投入 10 件との重複回避。`content/ranking-stock/pref-ranking-1/2026-08-initial/research.md` 参照）→ データ検証・執筆・人間レビュー・両環境投入
    - [ ] **17-5b** アカウント開設・BGM 調達・セット登録
      - 内容: アカウント開設（`daigoro_ranking`）・Secret 作成 → BGM 調達・登録（`time_slot` NULL の汎用曲 3〜5 曲）→ DB セット登録の残り（`sns_accounts`・`caption_templates`・`prompt_configs`〔単一スロット・`duration_seconds: 30`〕= [operation.html](app/operation.html) セクション 2.1。`batch_sets` 行は 17-3 で登録済み）
    - [ ] **17-5c** 初期 14 ネタの 30 秒版ビルド・全数レビュー・稼働開始
      - 内容: 背景 imagegen 14 枚 → 30 秒版 14 本のビルド → 全数人間レビュー → S3 配置 + 両環境 DB 反映 → Scheduler 追加（画像生成 1 件〔evening 20:00 JST〕+ インサイト収集 2 件）・バックログ 0 件確認 → 手動実行確認・稼働開始
- [ ] **17-6** テスト投稿・効果確認（尺比較は行わない）
  - **内容を変更した（2026-08-24）**: 尺 30 秒確定に伴い、2 尺比較（昼 20 秒 / 夜 30 秒のスロット分離・検証後半のスワップ）は**中止**。目的を**セットとしての初速確認**に変更する: 自動投稿の安定稼働（欠番なし）・インサイト自動収集の確認・完走率 / 保存 / フォロワー初速の観測 → 定常運用への移行判断。増便（1 日 2 回へ）や 20 秒尺の再検討は初速を見てからの判断とし、必要になれば 20 秒版は実装済みのため、ビルドと `parameters` へのスロット追加だけで再開できる
- [ ] **17-7** ランキングストック補充のスキル化 + 残り 16 件の投入（実地テスト）
  - **順序制約が消えた（2026-08-24）**: 旧計画の「17-6 の後」は勝ち尺の確定待ち（確定でビルド対象と在庫の数え方が変わるため）だったが、尺が 30 秒で確定したため待つ理由がない。**17-5 完了後すぐ着手可・17-6 と並行**。在庫 14 件は 2 週間分のため、**稼働開始から 2 週間以内に完了させる**（LRU 一周 = 再利用 WARNING を出さないため）
  - 内容: `/quiz-stock-replenish` を前例に `.claude/skills/ranking-stock-replenish/` を作成する。一次資料は **[content/ranking-stock/pref-ranking-1/WRITING-NOTES.md](../content/ranking-stock/pref-ranking-1/WRITING-NOTES.md)**（17-3 で確立した執筆・レビュー・投入の勘所）+ [operation.html](app/operation.html) セクション 3（手順の正）+ [方式設計書](app/generators/ranking-prebuilt.html) セクション 8。**30 秒単尺・週 7 件補充の前提で書く**。**実地テストとして、そのスキルでネタ帳の残り 16 件を投入・ビルドする**（17-3 の「書いただけで検証しないスキルにしない」〔ユーザー判断 2026-08-11〕の趣旨を回復。機械検証・レビューで不採用が出た場合は採否ルールで代替を選定）。完了条件 = ストック 30 件・30 秒版ビルド済みが両環境に揃うこと。以後は週次補充（7 件目安・在庫水位 14 件以上）の定常運用へ
  - 注意: 16 件のネタ選定では 17-3 で確立した採否ルール（TOP6 に同値があるネタは不採用 / 時刻・時分の指標は現行データモデルに載らない）と、既投入分とのテーマ・出典の重複回避が要る

※ 17-3 以降のステップ分割・Codex 委譲割当は 17-2 の設計 Fix 時に見直し済み（2026-08-10。割当は「ステップ別の利用モデル・エフォートレベル」表を参照）。**17-5〜17-7 は 2026-08-24 の計画見直しで再編済み**（投稿 1 日 1 回・尺 30 秒確定・稼働開始条件 14 投稿分）

---

## Phase XX: プラットフォーム展開

**ゴール**: 当たりの兆しが出たセットを他プラットフォームへ展開する

> 旧 Phase 17。2026-08-10 に第 3 セット構築を Phase 17 として優先するユーザー決定に伴い番号を退避（着手判断時に採番する）

- 骨子: TikTok を第一候補とする（動画資産の転用。Content Posting API のアプリ審査等の要件は着手時に調査）。X は当面見送り（判断根拠は事業戦略書セクション 5）。着手判断の基準は「勝負セットで M2 到達が目安」。ステップ展開は着手判断時に行う

---

## トラブルシューティングログ

各ステップで発生した問題と解決策を記録する。

| 日付 | Phase-Step | 問題 | 解決策 |
|---|---|---|---|
| 2026-07-18 | 10-3 | V001 の複合 FK 5 本（`generated_images` 2 本・`posts` 3 本）に付けていた `COMMENT` 句が MySQL 構文違反（FOREIGN KEY 制約は COMMENT をサポートしない）。V001 は実 MySQL で未実行のため潜伏しており、10-4 の適用時に ERROR 1064 で失敗するところだった | 10-3 の構文検証（sqlfluff・MySQL 方言）で検出。COMMENT の記載内容を FK 直前の行コメントへ移動して解消（`UNIQUE KEY`・`KEY` の COMMENT は正当な構文のため残置）。全 9 テーブルのパース成功を再検証済み |
| 2026-07-18 | 10-4 | Query Editor での V001 初回実行が全 9 文 `Incorrect database name 'acps '; Error code: 1102` で失敗。接続ダイアログのデータベース名に末尾空白が入っていた（コピペ起因）。テーブルは 1 つも作られない | データベース名を空白なしの `acps` で入力し直して再実行 → 全文成功。Query Editor の接続ダイアログは入力値の前後空白をトリムしないため、DB 名はコピペ後に空白を確認する |
| 2026-07-29 | 15-10 | `docker exec -i acps-mysql mysql ... < V004.sql` で DDL を適用すると、mysql クライアントの既定文字コードが latin1 のため日本語 COMMENT が二重エンコードで保存される（utf8mb4 クライアントで読むと文字化け。latin1 クライアントで読むと一見正常に見えるため気づきにくい） | カラム・インデックスを一度戻してから `--default-character-set=utf8mb4` を付けて再適用し解消。ローカル MySQL への SQL ファイル適用・日本語を含む SQL 実行では同オプションを必須とする（初回起動時の自動適用〔docker-entrypoint-initdb.d〕はサーバー設定で utf8mb4 になるため影響なし） |
| 2026-08-06 | 定常運用（Phase 16 前段） | 8/4 07:30 JST の logic-training-1 画像生成が、クイズ検証の再生成上限到達で SFN 失敗（ECS 3 試行 × 各 6 回が全滅。NG 理由は independent LLM validation failed / unhashable type: 'dict' / machine_spec must have exactly one solution の混在）。`generated_media` 0 件の generation_run（id=44）が残り、投稿対象選定が「最古の actionable」優先のため以後の投稿バッチが run 44 を選び続けて RuntimeError（No generated media was found）で失敗。8/4 12:40〜8/6 に `acps-prod-sns-posting-sfn-failed` が定時実行のたびに発報し、logic-training-1 の投稿が 8/3 21:00 を最後に停止（設計課題リスト 2026-07-22 の予見どおりの事象。fantasy-animals-1 は影響なし） | operation.html 4.2/4.3 の stale データ手順で復旧。未投稿の generation_run 8 件（44, 47, 48, 51, 52, 53, 55, 56。44 のみメディア 0 件、他 7 件は未投稿リール各 1 件 = 鮮度切れのため破棄と判断）に posts 行（set_id=2・sns_account_id=2・media_type='reel'・status='failed'・error_message に手動対象外化の旨）を Data API で INSERT し投稿対象外化。あわせて logic-training-1 の生成スケジュール 3 本を Phase 16 リリースまで DISABLED 化（CLI 即時 + CDK `enabled: false` を反映し「手動 DISABLE が deploy で戻る」事故を防止）。恒久対策（選定条件へのメディア存在チェック追加）は Phase 16 でストック方式へ転換するため見送り |
| 2026-08-23 | 定常運用（Phase 17 進行中） | 8/23 12:33 JST の logic-training-1 昼スロットで、Instagram のメディアコンテナが処理中に `status_code=ERROR` を返し、リールが未投稿のまま `posts.id=174`（generation_run 119・`media_type='reel'`）が `failed` になった（コンテナ ID `18064413008566231`・`api_response` は `{"id":"...","status_code":"ERROR"}` のみで理由は不明）。ストーリーズ行はリール成功後にしか作られないため未作成。SFN の Retry 2 回目は投稿対象を「なし」と判定して 0 件で正常終了したため、**Step Functions 実行は SUCCEEDED のまま昼の投稿だけが欠番**になり、気づけたのは ECS タスク異常終了通知のみだった | 動画は原因ではないことを確認（run 119 と前日成功の run 118 を ffprobe・フルデコードで比較 → H.264 High / yuv420p / 1080x1920 / 30fps / 16.00 秒 / AAC 48kHz stereo が完全一致、デコード警告の数も同一。音声ストリームの MD5 は 8/22 昼の run 115〔投稿成功〕と一致 = 同一 BGM）。過去 10 日で初発のため Instagram 側の一過性エラーと判断し、[operation.html](app/operation.html) セクション 4.2 の手順で `posts.id=174` を `pending` へ戻して（`platform_container_id` 等をクリア）SNS 投稿 SFN を手動実行して復旧（2026-08-23 15:51 JST 起動 → SUCCEEDED。リール `posts.id=174` / ストーリーズ `posts.id=175` とも `success`・バックログ 0 件に復帰）。失敗理由が記録されない点（ポーリングが `fields=status_code` のみ）と「SFN 成功・投稿 0 件」を検知できない点は設計課題リスト 2026-08-23 の 2 件に記録 |

## 設計課題リスト

設計レビューで出た改善提案や、一時 Fix 時に持ち越した論点を記録する（運用ルールは [docs/index.html](index.html) セクション 4 を参照）。解消した課題は [development-log.md](development-log.md) 末尾の「設計課題リスト（解消済み）」へ移す（2026-08-09 の計画書整理で導入）。

| 日付 | 対象ドキュメント | 課題 | 対応方針 | 対応時期 |
|---|---|---|---|---|
| 2026-07-07 | docs/app/operation.html | stale データ（`pending`/`container_created` のまま残った生成実行）の検知は専用アラームを持たず、既存の失敗通知を起点にした手動クエリ確認に依存する | セット数・投稿頻度が増えて見落としリスクが高まった場合、stale 行数を CloudWatch カスタムメトリクスとして発行する仕組みの導入を検討する。**15-16 棚卸し（2026-07-29）: 据え置き**（logic-training-1 投入で 1 日 4 実行へ増えたが発生実績なし。失敗通知起点の運用を継続） | 未定（将来拡張） |
| 2026-07-12 | docs/infra/workflow.html | 将来セット間のデプロイ分離（セットごとに使うタスク定義リビジョンを Scheduler payload で固定し、新セットの開発中も既存セットは検証済みイメージで動かし続ける方式）が必要になった場合、Step Functions の RunTask がタスク定義を入力から受け取れる形になっていれば、Scheduler の設定値追加だけで移行できる | **結論（2026-07-15、Phase 10-1 で検討済み）**: 継ぎ目は設けない。デプロイ分離自体を導入しないと決定済みであり、現行のシンプルな family 名参照を維持する。導入を判断する時点で、SFN 改修（タスク定義を入力から受け取る形）とセットで行う | デプロイ分離導入時 |
| 2026-07-14 | services/db-readiness-check | 6-1 で `shared/acps_shared` に DB 接続共通モジュールを作成したが、db-readiness-check は移植元の `app/secrets.py` / `app/db.py` を持ったままで、Secret パース処理が二重管理になっている。統合には db-readiness-check の Dockerfile をルートコンテキスト方式（`COPY shared/`）に変更してイメージを再ビルド・再 push する必要があるため 6-1 では見送った | **6-2 で判断（2026-07-14）**: 統合は見送り。db-readiness-check は Phase 3 で検証済み・全ワークフローの先頭ステートで稼働中の安定コンポーネントであり、二重管理の Secret パース処理は Phase 10 まで双方とも変更予定がない一方、統合にはイメージ再ビルド・再 push・AWS 上の再検証が必要でリスクに見合う実益がないため。db-readiness-check に次に機能変更で手を入れるタイミングで、ルートコンテキスト方式への変更と合わせて統合を再検討する | Phase 10 以降（再検討） |
| 2026-07-15 | docs/infra/security.html | 画像生成 API の複数プロバイダを併用する場合、単一 Secret `acps/{env}/image/api-key` では足りない（生成方式は差し替え可能な strategy 構造のため、将来プロバイダが並存しうる） | provider 軸の Secret 命名拡張（例 `acps/{env}/image/<provider>/api-key`）を security.html の規約改定・IAM プレフィックス確認とセットで、2 プロバイダ併用が現実になった時点で検討する。**15-16 棚卸し（2026-07-29）で補記**: gpt-quiz-multicut は LLM テキスト API（Responses）も同一 Secret の OpenAI キーを共用しており、名称は `image` だが実態は「OpenAI API キー」になっている。provider 拡張の検討時は名称の実態合わせも合わせて扱う | 2 プロバイダ併用時 |
| 2026-07-15 | docs/app/batch-flow.html | 2 つ目の SNS プラットフォーム追加時、batch-flow.html セクション 3 の Instagram 固有部分（コンテナ作成 → パブリッシュの 2 段階フロー、`published_unconfirmed` の判定条件等）をどう分冊・共通化するかの設計書構造が未定義 | 生成方式の 3 層分冊と同様の考え方（共通フロー + プラットフォーム別分冊）を軸に、追加が現実になった時点で確定する | 2 つ目のプラットフォーム追加時 |
| 2026-07-22 | docs/app/batch-flow.html セクション 3.1, services/sns-post-batch/app/target_selection.py | 投稿対象の選定条件は「有効アカウントに終端状態の posts がない最古の generation_run」のみで、生成画像の存在を条件にしていない。画像生成が全滅して `generated_media`（14-5 のリネーム前は `generated_images`）0 件のまま終わった generation_run が残ると、以後の投稿バッチはその実行を選び続けて RuntimeError（No generated image was found）で失敗し、手動クリーンアップまで新しい生成実行の投稿がブロックされる。フェイルラウド方針（失敗アラーム → stale データ運用による手動復旧）とは整合しており blocker ではない（12-4/12-5 後の Sonnet 作業チェックのコードレビューで検出） | 発生した場合は stale データ運用（[docs/app/operation.html](app/operation.html) セクション 4）で該当 generation_run を手動クリーンアップする。発生が繰り返して運用負荷になる場合は、選定条件に「`generated_media` が 1 件以上存在する」を加える改修（batch-flow.html 3.1 の更新とセット）を検討する。**14-12 の棚卸し時点で発生実績なし**（動画方式では ffmpeg 失敗という新しい全滅経路が増えたため、引き続き観察する）。**15-16 棚卸し（2026-07-29）: 引き続き発生実績なし・据え置き**（gpt-quiz-multicut では再生成上限到達・イラスト生成失敗・組版の収まり不足という全滅経路がさらに増えており、SFN Retry の 2 回目で完了する場合もこの状態を経由しうるため観察は継続） | Phase 16 以降（発生実績を見て判断） |
| 2026-07-24 | docs/app/operation.html | AI 開示ハッシュタグ `#AIart` の付与はキャプションテンプレートに含める運用ルール（テンプレート登録時のチェックリスト）のみで担保しており、システム的な検証（テンプレートに含まれているかのチェックや投稿時の自動付与）はない。テンプレート差し替え時の付け忘れは目視でしか気づけない | セット数・テンプレート更新頻度が増えた場合、投稿バッチ側でのタグ存在チェック（警告ログ）または自動付与の導入を検討する（管理画面の将来拡張と合わせて判断） | 未定（将来拡張） |
| 2026-07-25 | services/sns-post-batch/app/target_selection.py, docs/app/batch-flow.html セクション 3.1 | 投稿対象は「終端状態の posts を持たない**最も古い** generation_run」のため、未投稿の run が 1 件でも滞留すると以後の投稿が恒常的に 1 実行分遅れ、**自然解消しない**（毎回 1 件ずつ古い方を消化するだけ）。14-10 の全チェーン実行で、生成したリールではなく 1 本前の静止画が投稿されて判明した。投稿自体は成功し続けるため、失敗アラームでは気づけない | 14-10 では SNS 投稿 SFN を単体実行してバックログを消化し解消した（生成なしで投稿 1 本のみ実行できる）。再発時も同じ手順で解消できる。定期的に「未投稿 run の件数」を確認する運用、または滞留件数の可視化（2026-07-07 の stale データ検知の課題と同じ枠組み）を、頻発するようなら検討する。**14-12 で 1 日 1 回に減らしたため、1 件の滞留がそのまま「毎日 1 日遅れの投稿」になり気づきにくさが増した**点に注意（14-12 時点のバックログは 0 件）。**15-16 棚卸し（2026-07-29）: 据え置き**（15-15 で「デプロイ・Scheduler 追加前にアプリの実クエリでバックログ 0 件を確認する」運用を実施済み = 予防手順として有効。logic-training-1 は 1 日 3 回のため滞留時の遅れは 1 スロット分で済むが、滞留がスロットのだし分け〔朝の回に夜向けコンテンツが投稿される等〕のズレとして現れる点が新しい） | Phase 16 以降（発生頻度を見て判断） |
| 2026-07-26 | docs/app/operation.html, docs/infra/workflow.html | 14-12 で投稿頻度を 1 日 1 回に減らしたため、**1 回の失敗がその日の投稿ゼロに直結する**（1 日 3 回のときは残り 2 回で自然にカバーされていた）。失敗アラーム受信後に当日中へ復旧させる手順（画像生成 SFN の手動再実行の可否・締切時刻の目安）が運用ルールとして明文化されていない | 失敗アラームが実際に鳴った際の対応で運用感を掴んでから、operation.html の失敗時ポリシー（セクション 4）に「当日リカバリの手順と判断基準」を追記する。頻度をさらに上げる場合は不要になる可能性がある。**15-16 棚卸し（2026-07-29）: 据え置き**（対象は fantasy-animals-1 のみ。logic-training-1 は 1 日 3 回のため 1 回の失敗が投稿ゼロに直結しない。ただしクイズ方式は再生成上限到達というフェイルラウド経路を持つため、失敗実績はこちらで先に出る可能性が高い） | 失敗実績を見て判断 |
| 2026-07-27 | docs/app/data-model.html セクション 4.9（`quiz_items`） | クイズ方式の検証・再生成の過程メタ（再生成回数・自己検証の結果・類似度スコア）は DB に記録しない割り切りとした（観測は CloudWatch Logs のみ）。検証チェーンの実際の NG 率・再生成頻度が運用開始まで見えない | Phase 16 のインサイト収集で問題別パフォーマンス分析を設計する際、必要になれば `quiz_items` へのカラム追加（または検証ログテーブル新設）を検討する。それまでは CloudWatch Logs の構造化ログで観測する。**16-1（2026-08-01）: 生成チェーン休止（ストックのみ方式への転換）により過程メタ自体が発生しなくなり、課題の対象は大幅に縮小**（問題別パフォーマンス分析の突合キーは 16-1 で追加した `quiz_items.stock_item_id` が担う。生成経路を再開する場合のみ再燃） | Phase 16 で判断（16-1 で対象縮小） |
| 2026-08-10 | services/sns-post-batch/app/instagram_api.py, shared/acps_shared | 16-7 の設計 Fix で、Instagram クライアントの汎用層（HTTP / エラー処理）と SNS Secret 名規約を `shared/acps_shared` へ昇格して insights-batch から使う方針とした（16-9 で実施）。sns-post-batch は当面ローカル実装のままのため、汎用層が一時的に二重管理になる。また sns-post-batch の Graph API バージョンは v21.0 のままで、insights-batch（v25.0 系でピン留め予定）とバージョンが並存する | sns-post-batch に次に機能変更で手を入れるタイミングで、shared 昇格版への載せ替えと API バージョン引き上げ（v25.0 系。16-6 (7) の廃止メトリクス名は投稿系エンドポイントには影響しないため動作互換の見込み）をセットで実施する（db-readiness-check の二重管理〔2026-07-14 の項〕と同じ整理） | sns-post-batch の次回改修時 |
| 2026-08-10 | docs/app/data-model.html セクション 4.13, docs/app/generators/ranking-prebuilt.html | ランキングの値は `ranking_data.value`（数値）+ `content_fields.value_suffix`（単位）で表示・読み上げする構造のため、**時刻や「時間.分」形式の指標が載らない**。17-3 のネタ検証で、社会生活基本調査の「夜ふかし（平均就寝時刻 23:39）」「食事時間（1時間45分）」が採用できず候補から外れた。分へ換算すれば数値化できるが、版面表示（`105分`）と読み上げの自然さをどうするかの判断が必要 | 第 2 バッチ以降でこの系統のネタを採用したくなった時点で、①分換算 + `value_suffix="分"` で押し切る ②`content_fields` に表示用の整形文字列を持たせる（`ranking_data` は数値のまま = 言語非依存を維持） のどちらかを選ぶ。当面は数値指標（金額・件数・率）のネタで在庫を賄えるため据え置き | 第 2 バッチ以降（必要になった時点） |
| 2026-08-23 | services/sns-post-batch/app/instagram_api.py | コンテナのポーリングが `fields=status_code` しか取得していないため、`status_code=ERROR` になった**理由が一切残らない**（`api_response` に保存されるのも `{"id":"...","status_code":"ERROR"}` のみ）。Graph API は `status` フィールドに `Error: 2207026 - Video format not supported` 形式の理由文字列を返すが、これを取っていない。また、コンテナ ERROR はパブリッシュ前の段階（二重投稿リスクなし）にもかかわらずアプリ内リトライを持たず、1 回の ERROR でその生成実行が恒久的に欠番になる（2026-08-23 のトラブルシューティングログの事象） | ①ポーリングの `fields` に `status` を追加し、ログと `api_response` に理由文字列を残す（数行の修正。次に同種の失敗が起きた際の切り分けに必須）。②コンテナ作成からやり直すアプリ内リトライ（1〜2 回）は、一過性エラーの再発頻度を見てから判断する。**2026-08-10 の項（shared 昇格・Graph API v25.0 系への引き上げ）と同じ「sns-post-batch の次回改修時」にまとめて実施する** | sns-post-batch の次回改修時（① は先行実施も可） |
| 2026-08-23 | docs/app/operation.html, docs/infra/workflow.html | 投稿対象の決定が「終端状態の posts を持たない最古の generation_run」であるため、**リールが `failed` になった直後の SFN Retry は「対象なし」と判定して 0 件処理で正常終了する**。結果として Step Functions 実行は SUCCEEDED になり、SFN 失敗アラームでは投稿の欠番に気づけない（2026-08-23 の事象では ECS タスク異常終了通知だけが手がかりだった）。`batch_execution_logs` に `records_processed=0` は残るが、これを見る運用・アラームがない | `batch_execution_logs` の `records_processed=0`（かつ当日の投稿 success が 0 件）を検知する仕組み（CloudWatch カスタムメトリクス、または ECS タスク異常終了通知の受信時チェック手順の明文化）を検討する。当面は **ECS タスク異常終了通知を受けたら投稿の成否を必ず確認する**運用で代替する（2026-07-07 の stale データ検知・2026-07-25 のバックログ滞留と同じ「可視化がない」系の課題であり、まとめて扱う） | 未定（発生頻度を見て判断。可視化の枠組みは 2026-07-07 の項と合わせて検討） |
| 2026-08-24 | docs/app/sets/logic-training-1.html セクション 5（キャプションテンプレート） | 16-4d の昼スロット停止後も、キャプションテンプレートの固定ハッシュタグに `#フェルミ推定` が残っており、朝・夜の投稿にも付き続ける（実害は軽微だが出題内容と合わない）。変更には `caption_templates` の DB 差し替え（旧テンプレート `is_active=0` 化）が必要 | 昼スロットの再開判断時、またはテンプレートを他の理由で差し替えるタイミングで合わせて見直す（単独での差し替えはしない） | 昼スロット再開判断時 |
