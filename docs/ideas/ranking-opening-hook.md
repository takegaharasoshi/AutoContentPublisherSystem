---
title: pref-ranking-1 動画の冒頭フック改善（ミュート離脱対策）
slug: ranking-opening-hook
status: 採用            # inbox | 醸成中 | 再醸成待ち | 待機 | 採用 | 見送り
kind: 単発               # 単発 | 構想
created: 2026-08-28
updated: 2026-08-28
condition: ""            # 再醸成待ち・待機のとき必須（再検討トリガー / 落とし込み条件）
parent: ""               # 構想の子の場合、親の slug
children: []             # 構想の場合、子の slug のリスト
detail: ""               # 詳細 HTML を作ったらファイル名（<slug>.html）
disposition: "採用（2026-08-28・1 回の壁打ちで着地）。セットレーンで完結するため待機を挟まず、docs/plans/pref-ranking-1.html のバックログ（2026-08-28 の項）へ転記済み。実装時に docs/app/generators/ranking-prebuilt.html セクション 8.2 のタイムライン記述を更新する"
---

## 要旨

pref-ranking-1 の 30 秒版動画は、実測で **0.00〜5.00 秒が実質完全な静止画**（`intro` 89f + `teaser` 61f = 150f）。
地図ズームはこの区間 1.04 固定、背景 ken-burns は 900f で 1.0→1.07 と知覚できず、地図に動きが出るのは 5.00 秒（5 位スピン開始）、
最初の「確定」は 6.00 秒（停止フラッシュ）。17-4a で版面の情報（タイトル帯・地図・全国平均プレート・煽り吹き出し）は
0 秒から出す判断が入っているため、課題は「情報がない」ではなく **「情報はあるが動きがゼロ」**。
ミュート視聴者が冒頭を静止画と判定してスキップしている、というのが仮説。

**対策（採用）= A + B の同時投入**。
- **A. 予熱スピン**: `intro`・`teaser` 区間でも地図のルーレットを回す。**スピン速度はフレーム 0 から本番と同等の最高速**
  （実測カーブで崖が 0〜2 秒にあると判明したため。速度を 5.00 秒へ向けてランプさせる当初案は撤回した）。
  可読性（タイトル・全国平均プレート・煽り文）と 5.00 秒の変化点は**発光強度でランプさせて作る** —
  予熱区間は `drop-shadow` を淡く抑え、5.00 秒の本番スピンで強度を上げ、停止フラッシュで最大にする。
  **速さと眩しさを分離する**のが設計の核。
- **B. `teaser` の視覚化**: 煽り吹き出しにモーション（ポップイン・「？」の脈動等）を与え、
  ミュートでも「予想しろ」という参加要求が届くようにする。A が「動きの欠如」を埋めるのに対し、B は
  「17-3 で `teaser` を全ネタ必須にした狙いがミュート勢に届いていない」という別の穴を埋める。
  **モーションは 0〜1 秒に集中させる**（5 秒かけて見せる動きは崖より後になり届かない）。

在庫は**全本作り直す**（使用済みを含む全 30 件。ユーザー判断）。`ranking-prebuilt` は LRU 順繰りで、
`last_used_at IS NOT NULL` の行を選んだ場合も WARNING ログを出すだけで再投稿するため、使用済み在庫も再び表に出る。
レーンは**セットレーン**（`content/<set>/` 配下 + セット専用方式設計書 + セット計画書で完結）。

## 前提条件・再検討トリガー

- 実装は `content/video-build/pref-ranking-1/remotion/` 配下で完結する見込み。A は `stageAt` の
  `frame < rounds[5].start` 分岐（現在 `lit=null` を返す）と `spinTicks` の間隔式に予熱区間を足す改修。
  B は煽り吹き出しコンポーネントの改修。タイムライン定数（`scripts/build_timeline.py` の `DURATION_SPECS`）は**変えない**
  — 総尺・シーン配分には手を入れず、既存区間に動きを足すだけにする
- コストの本体は**未使用在庫の再ビルド**（2026-08-28 時点で 28 件）+ S3・DB の差し替え。
  BGM 1 曲運用のミュート検知時と同じ「在庫全本の再ビルド」オペレーション（セット別設計書セクション 6 の decision）に相当する
- **変更日をセット記録に必ず残す**。16-5 効果検証は「2026-08-08 以降の投稿を母集団に絶対水準と改善傾向を見る」設計
  （16-11 で前後比較は放棄済み）なので改善の投入自体は矛盾しないが、変更日が分からないと推移の解釈ができなくなる
- 効果の判定材料: `reels_skip_rate`（Graph API 取得可）と視聴維持カーブ（アプリ画面の目視のみ。16-6 の調査結果）

## 原文メモ

### 2026-08-28

> アイデアです。
> pref-ranking-1
> 何ですが、最初4秒くらい音声のみで画面上ほぼ動きがない状態が続いています。
> そのため、ミュートで見ている人は、一瞬でスキップしてしまうのかなと思いました。
> 改善したいですが、ジャストアイデアとして、最初から都道府県ルーレットを光らせると、面白いかなと思いました。

## 壁打ち記録

### 2026-08-28

**1 回の壁打ちで採用まで着地した。**

- **現状の実測で仮説を裏付けた**: 30 秒版の 0.00〜5.00 秒は動くものが何もない（`mapZoomAt` はこの区間 1.04 固定、
  `Backdrop` の ken-burns は 1 秒あたり 0.23% で知覚不能）。体感 4 秒という指摘に対し、実際は**動きまで 5.0 秒・確定まで 6.0 秒**。
  17-4a の「冒頭が空の版面はスキップされる」判断で情報は 0 秒から出ているため、残っていた穴は動きだけ、と論点を絞り込んだ
- **前提の検証手段を確認した**: 視聴維持カーブはアプリ画面で目視可能（16-6）で、ユーザーが実際に確認できることを確認。
  `reels_skip_rate` は API 取得可
- **ユーザー案（冒頭からルーレットを光らせる）は採用したが、素朴な実装は 2 つ問題があると指摘した**:
  ① 5 秒間フル点滅はミュート勢に読ませたいタイトル・全国平均プレート・煽り文の可読性と競合する
  ② 5 位スピン開始（5.0 秒）が変化点でなくなり「確定」の特別感が薄れる。
  → **加速する予熱スピン**（遅く淡く始めて本番スピンで最高速）に修正して両方を回避する形で採用
- **B（`teaser` の視覚化）を対案として追加し、ユーザー判断で A と同時投入**。理由は埋める穴が別（A = 動きの欠如、
  B = 参加要求がミュート勢に届いていない）で、どちらもセットレーンで完結し 1 回の再ビルドに相乗りできるため
- **却下した案**: 冒頭を削って 5 位発表を前倒しする案は、17-4d の実測で発話密度 79%・話速が設計上限 1.2 に到達済み・
  尺配分の余地も使い切っているため成立しない。削るなら `teaser` を捨てることになり、17-3 の判断の巻き戻しになる
- **レーン判定**: `docs/plans/index.html` の判定表で `content/<set>/` 配下（Remotion コンポジション含む）+ セット専用方式設計書 +
  セット計画書は**セットレーン**、用途例に「動画の演出・尺・構成のセット内改修」が明記されている。開発レーンの枠を消費しない
- **積み残し**: ユーザーが見た視聴維持カーブの読み（冒頭で崖になっているか、崖の位置は何秒か）を証跡として記録できていない。
  実装前に確認できれば、予熱スピンの加速カーブ設計（どこを一番強くするか）の根拠になる
  → **同日中にユーザーが Instagram アプリの実データを提示し、解消した（下記）**

### 2026-08-28（追記: 視聴維持カーブの実データを受けて設計を修正）

ユーザーが Instagram のリールインサイト「あなたのリール動画の再生時間」を 2 本分提示（サンプル数は少ない）。

| | ぎょうざ大好き都道府県 | ラーメン大好き都道府県 |
|---|---|---|
| 0:05 時点の維持率 | **18%** | **37%** |
| 崖の位置 | 0〜2 秒（0:02 で約 25%） | 0〜2 秒（0:02 で約 50%） |
| 5 秒以降 | ほぼ平坦（13〜15%）→ 末尾で落ちる | ほぼ平坦（28% 前後）→ 末尾で落ちる |
| 流入ソース | リールタブ 80.2% / 発見 19.8% | リールタブ 70.2% / 発見 29.8% |

読み取りと、そこから生じた判断:

- **崖は 0〜2 秒にあり、5 秒地点では既に平坦部に入っている**。当初採用した「遅く淡く始めて `teaser` 終わりに向けて加速し
  5.00 秒で最高速」というランプは、**損失が起きている場所とズレている**（5 秒に向けて盛り上げても、その頃には落ちる人は落ちきっている）。
  → **速度のランプを撤回し、スピン速度はフレーム 0 から最高速に固定。可読性と 5.00 秒の変化点は発光強度のランプで作る**、
  という「速さと眩しさの分離」へ設計変更した。B のモーションも 0〜1 秒に集中させる
- **5 秒を越えた視聴者はほぼ最後まで見る**（18%→13% / 37%→28%）。中身・尺・構成そのものは保っており、
  **勝負は冒頭 2 秒に集中している**。冒頭改善のレバレッジは当初想定より大きい
- **冒頭の版面・タイミングは 2 本で完全に同一なのに、5 秒維持率が 18% vs 37% と 2 倍差がついている**。
  差分はネタ / タイトル / 背景イラストと流入ソース構成のみ。**「動きの欠如」とは別の、おそらくより大きいレバーが
  ネタ・タイトルの引きにある**という示唆 → 独立アイデアとして inbox 捕捉した（[[ranking-topic-appeal]]）
- **再ビルド範囲を「未使用在庫 28 件」から「使用済みを含む全 30 件」へ拡大**（ユーザー指摘）。
  `ranking-prebuilt` セクション 4 の LRU 順繰りは在庫枯渇時に `last_used_at IS NOT NULL` の行を選び、
  WARNING ログを出すだけで再投稿するため、使用済み在庫も再び表に出る
